#include <iostream>
#include "Tensor.hpp"
using namespace ts;
using namespace std;

int main() {
    // Tensor<int> t1(3, new int[3]{2, 2, 2}, new int[8]{1, 2, 3, 4, 5, 6, 7, 8});
    // Tensor<int> t2(3, new int[3]{2, 2, 2}, new int[8]{1, 2, 3, 5, 4, 6, 7, 8});
    // Tensor<int> t3(t1 + t2);
    // cout<<t3<<endl;
    // Tensor<int> t4(3, new int[3]{8, 8, 8});
    // for (int i = 0; i < 512; i++) {
    //     t4.data[i] = i + 3;
    // }
    // cout<<t4<<endl<<t4.Min()<<" "<<t4.Max()<<endl;
    // cout << (t1 > t2) << (t1 >= t2) << (t1 < t2) << (t1 <= t2) << endl;
    return 0;
}